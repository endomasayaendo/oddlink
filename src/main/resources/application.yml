app:
  base-url: http://localhost:8080

spring:
  application:
    name: oddlink

  # PostgreSQL
  datasource:
    url: jdbc:postgresql://localhost:5432/${DB_NAME:oddlink}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  # JPA
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
